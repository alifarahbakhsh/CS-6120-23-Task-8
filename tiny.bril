@main(in: int) {
  v: int = const 5;
  w: int = const 4;
  w: int = const 5;
  u: int = add w v;
  x: int = add v w;
  b: bool = const false;
  print x;
  br b .l .m;
  .l:
    u: int = mul v v;
    jmp .m;
  .m:
    nop;
}
